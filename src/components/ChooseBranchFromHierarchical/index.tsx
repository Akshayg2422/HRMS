import React from 'react'
import { Modal,  } from '@components'

type HierarchicalProps = {
    model?: boolean;
    toggle?: () => void;
}

function index({ model, toggle }: HierarchicalProps) {
    return (
<></>
        // <DropDown/>
        // <Modal showModel={model} toggle={toggle}>

        // </Modal>
    )
}

export default index